{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["uid", "email", "createdAt"]
    },
    "Folder": {
      "title": "Folder",
      "description": "Folder metadata for organizing papers.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["id", "name", "createdAt"]
    },
    "Paper": {
      "title": "Paper",
      "description": "Research paper metadata and status.",
      "type": "object",
      "properties": {
        "author": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "url": { "type": "string" },
        "added": { "type": "string" },
        "fullTextStatus": { "type": "string" },
        "viewed": { "type": "string" },
        "fileType": { "type": "string" },
        "summary": { "type": "string" },
        "fileId": { "type": "string" },
        "mimetype": { "type": "string" },
        "extractedText": { "type": "string" },
        "folderId": { "type": "string" }
      },
      "required": ["title"]
    },
    "Chat": {
      "title": "Chat",
      "description": "Persistent chat history.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "messages": {
          "type": "array",
          "items": { "type": "object" }
        },
        "updatedAt": { "type": "number" }
      },
      "required": ["title", "messages", "updatedAt"]
    },
    "Session": {
      "title": "Session",
      "description": "Active workspace state.",
      "type": "object",
      "properties": {
        "tabs": {
          "type": "array",
          "items": { "type": "object" }
        },
        "activeTabId": { "type": "string" },
        "messages": {
          "type": "array",
          "items": { "type": "object" }
        }
      }
    },
    "Annotation": {
      "title": "Annotation",
      "description": "User highlights and notes on a document.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "fileId": { "type": "string" },
        "text": { "type": "string" },
        "comment": { "type": "string" },
        "page": { "type": "number" },
        "color": { "type": "string" },
        "timestamp": { "type": "number" },
        "uid": { "type": "string" }
      },
      "required": ["id", "fileId", "text", "page", "timestamp", "uid"]
    },
    "Visitor": {
      "title": "Visitor",
      "description": "Visitor who accessed a collaborative workspace.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "email": { "type": "string" },
        "lastActive": { "type": "number" }
      },
      "required": ["id", "displayName", "lastActive"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile document."
    },
    "/users/{userId}/annotations/{annotationId}": {
      "schema": "Annotation",
      "description": "User document annotations."
    },
    "/users/{userId}/folders/{folderId}": {
      "schema": "Folder",
      "description": "User's research folders."
    },
    "/users/{userId}/papers/{paperId}": {
      "schema": "Paper",
      "description": "User's research papers."
    },
    "/users/{userId}/chats/{chatId}": {
      "schema": "Chat",
      "description": "User's saved chat conversations."
    },
    "/users/{userId}/workspace/session": {
      "schema": "Session",
      "description": "User's current active workspace session state."
    },
    "/shared_workspaces/{workspaceId}": {
      "schema": "Session",
      "description": "Collaborative workspace session state."
    },
    "/shared_workspaces/{workspaceId}/folders/{folderId}": {
      "schema": "Folder",
      "description": "Folders belonging to a shared workspace."
    },
    "/shared_workspaces/{workspaceId}/papers/{paperId}": {
      "schema": "Paper",
      "description": "Papers belonging to a shared workspace."
    },
    "/shared_workspaces/{workspaceId}/chats/{chatId}": {
      "schema": "Chat",
      "description": "Chats belonging to a shared workspace."
    },
    "/shared_workspaces/{workspaceId}/annotations/{annotationId}": {
      "schema": "Annotation",
      "description": "Annotations belonging to a shared workspace."
    },
    "/shared_workspaces/{workspaceId}/visitors/{visitorId}": {
      "schema": "Visitor",
      "description": "Profile information of users who have accessed this collaborative workspace."
    },
    "/shared_libraries/{libraryId}/folders/{folderId}": {
      "schema": "Folder",
      "description": "Folders representing a shared public library."
    },
    "/shared_libraries/{libraryId}/papers/{paperId}": {
      "schema": "Paper",
      "description": "Papers representing a shared public library."
    }
  }
}
